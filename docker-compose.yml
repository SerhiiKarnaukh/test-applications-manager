services:
  app:
    container_name: portfolio
    build:
      context: .
    command: >
      sh -c "python manage.py wait_for_db &&
             python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    ports:
      - 8000:8000
    volumes:
      - .:/app
      - ./data/web:/vol/web
    environment:
      - SECRET_KEY=%bkii7@6vhj84@7sy^r-552ef)ztdv^
      - DEBUG=1
      - SQL_ENGINE=django.db.backends.postgresql
      - SQL_HOST=db
      - SQL_DATABASE=django_portfolio_dev
      - SQL_USER=django_user
      - SQL_PASSWORD=django_pass
      - EMAIL_HOST=changeme
      - EMAIL_PORT=587
      - EMAIL_HOST_USER=changeme
      - EMAIL_HOST_PASSWORD=changeme
      - EMAIL_USE_TLS=1
    depends_on:
      - db
  db:
    image: postgres:15-alpine
    container_name: portfolio-db
    volumes:
      - portfolio-postgres-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=django_portfolio_dev
      - POSTGRES_USER=django_user
      - POSTGRES_PASSWORD=django_pass

volumes:
   portfolio-postgres-data:
